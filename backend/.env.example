# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Atlas Connection (replace with your cluster details)
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>

# Authentication
JWT_SECRET=change_this_secret
JWT_EXPIRY=24h

# Stripe Integration
STRIPE_SECRET=sk_test_your_secret_here
STRIPE_WEBHOOK_SECRET=whsec_replace_if_using_webhooks

# Frontend URL (for CORS and redirects)
CLIENT_URL=http://localhost:5173

# SMTP settings for password reset emails (optional)
# SMTP_HOST=smtp.mailtrap.io
# SMTP_PORT=2525
# SMTP_USER=your_smtp_user
# SMTP_PASS=your_smtp_pass
# SMTP_FROM="Together <no-reply@example.com>"

# ===============================================
# PRODUCTION NOTES:
# ===============================================
# NODE_ENV should be 'production' when deployed
# CLIENT_URL should be your frontend domain (e.g., https://together.vercel.app)
# JWT_SECRET must be a long, random string (min 32 chars)
# STRIPE_SECRET should use 'sk_live_' keys for production
# All SMTP settings are required if password reset emails are enabled
